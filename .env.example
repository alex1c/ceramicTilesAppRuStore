# Copy to .env for local values. Never commit real keys.

# AppMetrica API key from the AppMetrica console (optional in v1.0).
EXPO_PUBLIC_APPMETRICA_API_KEY=

# When true, DevAnalyticsService logs events to the console.
EXPO_PUBLIC_ANALYTICS_DEV_MODE=true
