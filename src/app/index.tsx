import { TileCalculatorScreen } from '@/features/tile/tile-calculator-screen'

/** Root route — single-function app launches directly into the tile calculator. */
export default function RootCalculatorRoute() {
	return <TileCalculatorScreen />
}
